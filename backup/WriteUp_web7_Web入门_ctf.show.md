题目提示 `版本控制很重要，但不要部署到生产环境更重要。`，想到常见的版本控制软件 Git 的默认隐藏目录 `/.git`，访问直接获得 flag `ctfshow{f2113def-8a78-46c7-932f-d672b68be43c}`，这题目应该是简化了，省去了从 Git 版本控制历史记录中找到 flag 的过程。如果需要扒下 `/.git` 中的文件翻找历史记录的话可以使用 GitHacker 来方便操作。