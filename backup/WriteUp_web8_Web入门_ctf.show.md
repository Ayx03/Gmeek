提示和上一题一模一样，还是 `版本控制很重要，但不要部署到生产环境更重要。`，先访问 `./git` 试试，没有反应，尝试使用 GitHacker，使用 `python3 -m pip install -i https://pypi.org/simple/ GitHacker` 命令安装，发现一堆报错，访问 `./git/HEAD` 依旧返回 `web8:where is flag?`，想到其他版本管理软件比如 BitKeeper，又想到 SVN，访问 `./svn` 直接获得 flag `https://[8e6b932e-53cb-43b3-826a-40faebf2791d.challenge.ctf.show/.svn/](https://8e6b932e-53cb-43b3-826a-40faebf2791d.challenge.ctf.show/.svn/)`，应该也是简化过了